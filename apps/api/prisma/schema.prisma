generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Domain models will be added in later milestones.
// Keep Prisma wired so migrate/generate work from day one.
